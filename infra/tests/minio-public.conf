events {}

http {
    server {
        listen 8080;

        location / {
            proxy_set_header Host minio:9000;
            proxy_pass http://minio:9000/kirillwynn-integration-media/;
        }
    }
}
