# Django/Wagtail backend

This directory contains the new application backend. The Flask application
elsewhere under `backend/` is retained as legacy reference code until migration
work deliberately removes it.

The project targets Python 3.12, Django 5.2 LTS, Wagtail 7.4 LTS, PostgreSQL,
and Django REST Framework. See the repository README for local setup and
verification commands.
